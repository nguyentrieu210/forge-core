{
  "name": "forge",
  "version": "0.2.0",
  "private": true,
  "description": "Nền ERP tương thích Frappe chạy trên Cloudflare — kernel CloudForge + Desk/builder MetaForge",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "overrides": {
      "@metaforge/adapter-frappe": "workspace:*",
      "@metaforge/controls": "workspace:*",
      "@metaforge/core": "workspace:*",
      "@metaforge/shell": "workspace:*",
      "@metaforge/ui": "workspace:*",
      "@metaforge/views": "workspace:*",
      "esbuild": "0.25.12"
    }
  },
  "scripts": {
    "server:build": "pnpm --filter cloudforge run build",
    "server:test": "pnpm --filter cloudforge run test",
    "server:check": "pnpm --filter cloudforge run check:business-suite",
    "server:typecheck": "pnpm --filter cloudforge run typecheck:workers",
    "client:build": "pnpm --dir client run build",
    "client:typecheck": "pnpm --dir client run typecheck",
    "client:test": "pnpm --dir client run test",
    "typecheck": "pnpm run server:typecheck && pnpm run client:typecheck",
    "test": "pnpm run server:test && pnpm run client:test",
    "build": "pnpm run server:build && pnpm run client:build",
    "validate:gate": "node scripts/run-validation-gate.mjs",
    "validate:fast": "node scripts/run-validation-gate.mjs --risk FAST",
    "validate:standard": "node scripts/run-validation-gate.mjs --risk STANDARD",
    "validate:critical": "node scripts/run-validation-gate.mjs --risk CRITICAL",
    "validate:gate:test": "node --test scripts/validation-gate.test.mjs",
    "security:audit": "node server/scripts/dependency-audit.mjs",
    "verify": "pnpm run server:check && pnpm --filter cloudforge run test:workers && pnpm run client:typecheck && pnpm run client:test && pnpm run client:build",
    "release:manifest": "pnpm --filter cloudforge run release:manifest",
    "release:check": "node server/scripts/release-preflight.mjs && pnpm run security:audit && pnpm run verify && pnpm --filter cloudforge run verify:manifest"
  }
}
